import { RadioButtonObject } from './radio-button-object';

describe('RadioButtonObject', () => {
  it('should create an instance', () => {
    expect(new RadioButtonObject()).toBeTruthy();
  });
});
